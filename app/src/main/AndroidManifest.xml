<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.concertonline">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.transaksi.KonfirmasiActivity"></activity>
        <activity android:name=".ui.detail.DetailEventActivity" />
        <activity android:name=".ui.transaksi.PembayaranActivity" />
        <activity android:name=".ui.registrasi.FollowActivity" />
        <activity android:name=".ui.registrasi.LoginActivity" />
        <activity android:name=".ui.registrasi.VerifikasiActivity" />
        <activity android:name=".ui.registrasi.InputDataActivity" />
        <activity android:name=".ui.registrasi.DaftarActivity" />
        <activity android:name=".ui.transaksi.BuyActivity" />
        <activity android:name=".ui.detail_watch.DetailWatchActivity" />
        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_home" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>